# Ollama Configuration
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=

# Server Configuration
PORT=3000
HOST=0.0.0.0

# MCP Configuration (optional)
# Path to MCP config file (JSON format, same as Claude Code's MCP config)
MCP_CONFIG_PATH=

# Non-deferred tools (tools that are always loaded, not lazy-loaded)
# Comma-separated list of exact tool names
MCP_NON_DEFERRED_EXACT=

# Comma-separated list of glob patterns for tool names
# Example: github_*,database_*,filesystem_*
MCP_NON_DEFERRED_GLOB=

ANTHROPIC_API_KEY=