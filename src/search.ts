/**
 * Search Module Re-export
 *
 * This file re-exports from the search directory for backwards compatibility.
 * New code should import directly from "./search" (which resolves to ./search/index.ts).
 */

export * from "./search/index";
